Ciao <%= @resource.name %>!

Abbiamo ricevuto la richiesta di un link per la modifica della tua password; puoi modificare la password utilizzando il seguente link.

Modifica della mia password: <%= edit_password_url(@resource, reset_password_token: @token) %>

Se non hai richiesto di cambiare la password, ti preghiamo di ignorare questa email.

La password non cambierà a meno che tu non acceda al link qui sopra per crearne una nuova.

Carpe DiEM!
